import 'package:location/location.dart';

class UserMarker {
//  String id; //it is also the timestamp that this Marker is checked in //
  LocationData locationData;
  UserMarker(this.locationData);
}
